[

    { 
        "keys": ["ctrl+`"], "command": "toggle_terminus_panel", 
        "args": {
                "cmd": ["ipython", "-i", "$file"],
                "cwd": "${file_path:${folder}}"
                }
    },
]
